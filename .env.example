# Backend configuration (copy to `.env` for local dev)

# Optional: where to store all ingestion artifacts (captures, canonical, chunks, embeddings, vector index, releases, observability).
# Example: D:\ingestion\data
INGESTION_DATA_ROOT=./data

# Optional: override just the vector index root (defaults to `INGESTION_DATA_ROOT/vector_index`).
VECTOR_INDEX_ROOT=./data/vector_index

# Embeddings
# - `ollama`: uses local Ollama server (recommended if you have it running)
# - `deterministic`: no-network deterministic embeddings for offline dev/testing
RETRIEVAL_EMBED_PROVIDER=deterministic

# Ollama settings (only used when provider is `ollama`)
OLLAMA_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=mxbai-embed-large
OLLAMA_TIMEOUT_S=60

# Integrity signing (IMPORTANT in production):
# Used to sign capture content hashes. Set a long random value (do not commit it).
INGESTION_SIGNING_SECRET=CHANGE_ME_IN_PRODUCTION
